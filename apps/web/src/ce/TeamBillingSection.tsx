// The team's plan and invoices. A self-hosted instance sells nothing, so the section
// is not rendered; the hosted build resolves `@/cloud` to its own implementation.
export default function TeamBillingSection(_props: { teamId: number }) {
  return null;
}
