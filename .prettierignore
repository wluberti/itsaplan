# Prettier also respects .gitignore, so node_modules/.next/dist/coverage are already excluded.

# Lockfile
bun.lock

# Markdown docs (incl. release-please CHANGELOG.md)
*.md

# Generated Drizzle migrations and snapshots
packages/db/drizzle

# Generated better-auth schema
packages/db/src/schema/auth.ts

# Generated by Next on every dev run and build
apps/web/next-env.d.ts

# Helm chart: the templates are Go templates, not YAML, and the values follow
# Helm's own conventions. Checked by `helm lint`.
charts
